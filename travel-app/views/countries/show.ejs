<h1><%= country.name %></h1>
<p><%= country.description %></p>

<h2>Attractions</h2>

<% if (attractions.length > 0) { %>
  <ul>
    <% attractions.forEach(attraction => { %>
      <li>
        <strong><%= attraction.title %></strong> — 
        <%= attraction.description %> | Price: $<%= attraction.price %>
      </li>
    <% }) %>
  </ul>
<% } else { %>
  <p>No attractions found for this country.</p>
<% } %>
